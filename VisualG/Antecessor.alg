algoritmo "Escreva um algoritmo para ler um valor (do teclado) e escrever (na tela) o seu antecessor. "

var
   val: inteiro
inicio
   escreval("Digite o valor: ")
   leia(val)
   
   escreval("Antecessor de ",(val)," = ",(val-1))
fimalgoritmo
